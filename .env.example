# Copy this file to .env and fill in the values.
# .env is gitignored — never commit real secrets.
POSTGRES_PASSWORD=
DATABASE_URL=postgresql+psycopg://mealdoo:your_local_password_here@localhost:5432/mealdoo